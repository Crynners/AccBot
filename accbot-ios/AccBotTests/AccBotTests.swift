import XCTest
@testable import AccBot

final class AccBotTests: XCTestCase {
    func testPlaceholder() {
        // Placeholder test to prevent "no tests found" failure.
        // Replace with real tests as the app matures.
        XCTAssertTrue(true)
    }
}
